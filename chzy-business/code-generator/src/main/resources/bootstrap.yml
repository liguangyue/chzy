server:
  port: 7300



spring:
  application:
    name: code-generator
  cloud:
    nacos:
      config:
        server-addr: ${chzy.nacos.server-addr}
        file-extension: yml
        shared-dataids: common.yml
        refreshable-dataids: common.yml
      discovery:
        server-addr: ${chzy.nacos.server-addr}