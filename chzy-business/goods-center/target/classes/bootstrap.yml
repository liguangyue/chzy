server:
  port: 7002



spring:
  application:
    name: goods-center
  cloud:
    nacos:
      config:
        server-addr: ${chzy.nacos.server-addr}
        file-extension: yml
        shared-dataids: common.yml
        refreshable-dataids: common.yml
      discovery:
        server-addr: ${chzy.nacos.server-addr}